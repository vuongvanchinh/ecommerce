import React, { useMemo } from 'react'
// import { useSelector, useDispatch  } from 'react-redux'

const TableVariant = ({criterions, variants}) => {
    const r = useMemo(() => {
        let res=[]

        return res
    }, [criterions])

    return (
        <div>
            
        </div>
    )
}

export default TableVariant
