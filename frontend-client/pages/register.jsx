import React from 'react'

const Register = () => {
    return (
        <div>
            Register page
        </div>
    )
}

export default Register
Register.getLayout = (page) => {
    return page
  }