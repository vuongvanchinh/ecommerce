import React from 'react'

const ResetPasswod = () => {
    return (
        <div>
            Reset password page
        </div>
    )
}

export default ResetPasswod
ResetPasswod.getLayout = (page) => {
    return page
}